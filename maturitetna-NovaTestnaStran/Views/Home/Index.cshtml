@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<head>
    <meta charset="UTF-8">
    <title>Dobrodošli v ServerDash</title>
    <style>
        body {
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
            background-image: url('@Url.Content("~/Imgs/Background.jpg")');
            background-repeat: no-repeat;
            background-size: cover;
            font-family: Arial, sans-serif;
        }

        .glass {
            background: rgba(222, 226, 231, 0.407);
            box-shadow: 0 8px 20px 0 rgba(31, 38, 135, 0.37);
            backdrop-filter: blur(1px);
            -webkit-backdrop-filter: blur(5px);
            border-radius: 10px;
            border: 1px solid rgba(255, 255, 255, 0.18);
            padding: 20px;
            margin-top: 100px;
            display: flex;
            align-items: center;
        }

        .logo {
            width: 100px; 
            height: auto;
            margin-right: 20px;
        }

        .text-content {
            flex-grow: 1;
        }

        .btn-start-custom {
            background-color: #007bff;
            color: white;
            border: none;
            padding: 2px 8px;
            font-size: 1.3rem;
            border-radius: 5px;
            transition: background-color 0.3s;
            display: inline-block;
            margin: 0 auto;
        }

        .btn-start-custom:hover {
            background-color: #0056b3;
        }

        .button-container {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .server-logo {
            width: 300px; 
            height: auto; 
            display: block;
            margin-inline: auto; 
        }

        .server-link {
            text-decoration: none;
            color: inherit;
        }

        .glass-server {
            cursor: pointer;
            max-width: 200px; 
            margin: 0 auto; 
            padding: 10px; 
            background-color: #f8f9fa; 
            border-radius: 10px; 
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); 
            text-align: center;
        }

        h3 {
            margin: 0;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-10">
                <div class="glass">
                    <img src="~/Imgs/ServerLogo.png" alt="Server Logo" class="server-logo">
                    <div class="text-content">
                        <h1 class="mb-3">Dobrodošli v ServerDash</h1>
                        <p class="lead mb-3">Vaša centralna točka za upravljanje in nadzor strežnikov.</p>
                        <p>
                            Z uporabo ServerDash lahko enostavno spremljate stanje vaših strežnikov,
                            upravljate z viri in analizirate učinkovitost sistema.
                        </p>
                        <div class="button-container">
                            <a class="btn btn-start-custom" id="login" asp-area="Identity" asp-page="/Account/Login">Začnite zdaj</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</body>
