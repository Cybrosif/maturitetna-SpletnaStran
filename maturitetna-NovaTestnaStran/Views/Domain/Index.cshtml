@model IEnumerable<maturitetna_NovaTestnaStran.Models.DomainEntity>

@{
    ViewData["Title"] = "Index";
}
<style>
    body {
        background-color: #f8f9fa;
        background-image: url('@Url.Content("~/Imgs/Background.jpg")');
        background-repeat: no-repeat;
        background-size: cover;
    }
</style>

<h1>Index</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Ustvari</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Domain)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Domain)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-secondary">Uredi</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info">Podrobnosti</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">Briši</a>
                </td>
            </tr>
        }
    </tbody>
</table>
